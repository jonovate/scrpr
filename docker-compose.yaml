version: '3.8'
services:
  scrpr:
    image: scrpr:latest
    build: .
    env_file: .env
    restart: always
    depends_on:
      - webdriver
  webdriver:
    image: selenium/standalone-firefox
    container_name: selenium
    ports:
      - "4444:4444"
    expose: 
      - 4444
